# Required - These must be set (generated by `make setup` and `make docker-db`)
SESSION_SECRET=
DB_PASSWORD=

# Optional - These have sensible defaults
DB_USER=postgres
DB_HOST=localhost
DB_NAME=matcha
DB_PORT=5432
APP_PORT=3000

# Set to 'production' in production environment
NODE_ENV=development
